<%= form_tag('sessions#create') do |f|%>
	<%= label_tag :name%>
	<%= text_field_tag 'user[name]'%>
	<%= label_tag :password%>
	<%= text_field_tag 'user[password]'%>
	<%= submit_tag 'Sign In'%>
<%end%>